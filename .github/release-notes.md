APK 自动生成版本 beta0.0.3，修复澎湃 OS / MIUI 上���法选择文件夹的问题。

### 修复
- **文件夹路径解析增强**：兼容更多 ROM 的 DocumentsUI 返回格式（`primary`、`primary:`、`raw:`），解析失败时直接从 URI 路径段解码兜底
- **Shizuku 模式支持手动输入路径**：解析失败时可直接输入完整路径（如 `/storage/emulated/0/xxx`），不再依赖文件选择器
- **防呆**：误选「存储根目录」时给出明确提示，避免误操作

### 使用建议
- 澎湃 OS 上如果选择器仍提示无法解析，可切换到 Shizuku 模式后手动输入路径
- 仍推荐 SAF 模式（权限最低），先选文件夹再点「临时打开」
